# ===== PRODUCTION REQUIREMENTS FOR RENDER =====
# Optimized for free tier deployment
# Only includes dependencies actually used by ml_api_service_optimized.py

# Web Framework & Server
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0

# ML/Data Processing (for scikit-learn models)
scikit-learn==1.5.1
pandas==2.1.3
numpy==1.26.4
xgboost==2.0.3
joblib==1.4.2

# Explainability
shap==0.42.1

# Utilities
Pillow==10.0.0
requests==2.31.0
python-multipart==0.0.6
PyYAML==6.0
openpyxl==3.1.2
cryptography==41.0.3

# Note: Removed for production free tier:
# - tensorflow (NOT USED - causes timeout on Render free tier)
# - keras (NOT USED - causes timeout on Render free tier)
# - spacy (NOT USED - too heavy)
# - torch (NOT USED - too heavy)
# - transformers (NOT USED - too heavy)
# - lime (NOT USED - optional only)
# - prometheus-client (NOT USED - optional only)