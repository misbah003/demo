# ===== PRODUCTION REQUIREMENTS FOR RENDER =====
# Optimized for free tier deployment
# Exclude dev/test dependencies

# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0

# ML/Data Processing
scikit-learn==1.3.0
pandas==2.0.3
numpy==1.24.3
xgboost==1.7.6
joblib==1.3.2

# Deep Learning (TensorFlow lightweight)
tensorflow==2.13.0
keras==2.13.0

# Explainability
shap==0.42.1

# Utilities
Pillow==10.0.0
requests==2.31.0
python-multipart==0.0.6
PyYAML==6.0
openpyxl==3.1.2

# Monitoring (optional)
# prometheus-client==0.17.1

# Note: Removed for production free tier:
# - spacy (heavy, not required for basic predictions)
# - torch (heavy, only needed if using transformers)
# - transformers (heavy, not required for basic predictions)
# - cryptography (if not needed for specific features)

# These can be added if needed by uncommenting:
# spacy>=3.5.0
# transformers>=4.30.0
# torch>=2.0.0