# ===== ML API PRODUCTION REQUIREMENTS =====
# Optimized for Render free tier deployment
# Only includes dependencies actually used by ml_api_service_optimized.py

# Web Framework & Server
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0

# ML/Data Processing (for scikit-learn models)
Cython>=0.29.36
scikit-learn==1.5.1
pandas==2.2.3
numpy==2.1.0
xgboost==1.7.6
joblib==1.3.2

# Explainability
shap==0.42.1

# Email Service
sendgrid==6.10.0

# Utilities
Pillow==11.0.0
requests==2.31.0
python-multipart==0.0.6
PyYAML==6.0.2
openpyxl==3.1.2
cryptography==41.0.3
pydantic==2.9.0

# Note: Removed for production free tier (not used + cause timeout):
# - tensorflow (NOT USED by ml_api_service_optimized.py)
# - keras (NOT USED by ml_api_service_optimized.py)
# - spacy (NOT USED)
# - torch (NOT USED)
# - transformers (NOT USED)
# - lime (optional - not required)
# - prometheus-client (optional monitoring)